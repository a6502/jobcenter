# childjob
workflow "childjoberrortest" [
	in [
		"input" number;
	]
	out [
		"output" number;
	]
	wfomap [[
		$o{output} = $v{output};
	]]
	do [
		call raisetest2 [
			imap [[	$i{in} = "$a{input}" ]]
			omap [[ $v{output} = $o{out} * 1; ]]
		]
	]
]
